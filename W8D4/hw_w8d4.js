function titleize(names, callback) {
  let capitalized_names = names.map(name => 'x. ${name} Jingleheimer Schmidt');

  callback(capitalized_names);
}

function print_names);
}

titleize(["Mary", "Brian", "Leo"], (names) => {
  names.forEach(element => {
    console.log(element);
  }
  });